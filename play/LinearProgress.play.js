
import LinearProgress from './../client/components/LinearProgress.vue'
import { play } from 'vue-play'

play(LinearProgress)
  .name('LinearProgress')
  .displayName('LinearProgress')
  .add('default', '<LinearProgress />')
